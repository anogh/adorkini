/**
 * Adorkini Main JS
 */

document.addEventListener('DOMContentLoaded', function() {
    
    // Console Welcome
    console.log('Adorkini Theme Loaded');

    // Language Toggle Helper (if using JS-based toggle enhancement later)
    // Currently handled via URL param in PHP

    /**
     * Mobile Search Toggle (if implemented)
     */
    const searchTrigger = document.querySelector('.mobile-search-trigger');
    if (searchTrigger) {
        // Logic to open search modal/input
    }

});
